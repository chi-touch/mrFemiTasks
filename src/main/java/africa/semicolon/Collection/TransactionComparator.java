package africa.semicolon.Collection;

import java.util.Comparator;

public class TransactionComparator implements Comparator<Transactions> {

    @Override
    public int compare(Transactions previousTransaction, Transactions nextTransaction) {
    return 0;
    }
}
